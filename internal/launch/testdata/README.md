Source: `FFReStart/ReStart-HTTP-Server` `contracts/fixtures/launch-handoff.bin` at commit `a2c1680fc54d6fc5a9df00a2ab3e3f4281e16e32`.

The fixture contains only fixed interoperability test data and no production credential, token, key, or endpoint.

The canonical file is the raw protobuf message; the launcher adds one standard varint length prefix only when writing the message to game stdin.
